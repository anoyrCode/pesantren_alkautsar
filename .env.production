# Env produksi — nilai PUBLIK (bukan rahasia), sengaja di-commit
# agar ikut tertanam saat `npm run build` di Docker/CI.
# Vite otomatis membaca file ini saat build mode production.

VITE_GA4_ID=G-7RLM2CRNWX
VITE_SITE_URL=https://pesantrenalkautsar.id

# VITE_API_URL dikosongkan = pakai origin yang sama (nginx reverse proxy).
# Isi kalau backend dideploy di domain/host terpisah.
VITE_API_URL=
